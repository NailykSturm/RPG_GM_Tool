import { describe, expect, test } from "vitest";

const caller = "games.test.ts";
const sender = "test";
describe("games tests", () => {
    test("create a game", async () => {
        expect(true).toBe(true);
    });
});
